import { Label } from "./Label";
import { Options } from "./Options";
import { Root } from "./Root";
import { Select } from "./Select";

export const Form = {
    Root,
    Select,
    Options,
    Label,
};
